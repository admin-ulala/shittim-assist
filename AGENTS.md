# 项目协作规约

## 产品约定
- 名称：Shittim Assist / 什亭助手。MIT 开源，优先国服，Windows 与 Android 双端。
- 首批适配国服 B 服。用户指定 MuMu 第一个 ADB 地址 127.0.0.1:16416；另一个地址是同一实例，不得并发控制。
- 本次实机授权为简单了解界面；用户当日日常已清理，不执行领奖或消耗资源的任务。
- 用户已确认架构并授权开始实现、创建公开 GitHub 仓库及提交；无需重复询问这些事项。
- Flutter/Dart 共用界面与流程，Kotlin 实现 Android 服务，识别后端独立封装。
- 只通过屏幕识别与正常输入自动化。默认不抽卡、不购买、不消耗青辉石。
- 未实机验证的任务必须标记，不得用演示、固定延时或空实现冒充可用功能。

## 工程边界
- UI → 流程引擎 → 设备/识别接口，禁止 UI 绕过接口发送设备命令。
- 区服、语言、渠道分开建模；游戏包名与模板置于资源包。
- 输入坐标基于当前截图和内容区域，点击前核查画面与设备。
- 每个等待必须有超时，每个重试必须有限次；任务可暂停和取消。
- 同设备串行输入。恢复时重新识别，不重复提交有副作用的操作。
- 关键操作应有前置条件与后置验证，未知页面安全停止并保存证据。
- 配置校验、版本迁移、脱敏日志必须与功能同时实现。
- 新增依赖记录用途与许可证。游戏资产不因项目采用 MIT 自动成为 MIT。

## 验证与交付
- 运行 dart/flutter 格式、静态检查与相关测试；平台构建单独验证。
- 引擎变更测试取消、超时、重试和设备互斥；识别变更使用离线截图。
- 每项能力标识：已实现、回放验证、实机验证；不把编译成功等同实机通过。
- 中文用户文档，统一英文标识符。同步维护 docs/STATUS.md 和 README。
- 凭据不得进入源码、命令行参数、日志、测试资源和 Git 历史。认证使用凭据管理器。
- 不提交设备截图、账号数据、SDK、构建产物和个人配置。
- 后续用户明确的约束应同步此文件；不要擅自扩大为永久权限。
